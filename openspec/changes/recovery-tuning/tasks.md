# Tasks — recovery-tuning

- [x] 1. Spec: proposal + delta; `openspec validate recovery-tuning`
- [x] 2. Warm-recovery matrix: defaults / log.fragment.limit=512 / oms.lowlatency /
      combined; 2 runs each, mirrored order; `measurements.md`
- [x] 3. Verdict + ideas-file update — no knob promoted; combination reproducibly worse
- [x] 4. `bazel test //...` green; format + lint; PR
