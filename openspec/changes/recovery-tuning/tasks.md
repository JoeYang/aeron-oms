# Tasks — recovery-tuning

- [ ] 1. Spec: proposal; `openspec validate recovery-tuning`
- [ ] 2. Warm-recovery matrix: defaults / log.fragment.limit=512 / oms.lowlatency /
      combined; >=2 runs each, mirrored order; `measurements.md`
- [ ] 3. Verdict + ideas-file update; promote a knob only if it clears ~10%
- [ ] 4. `bazel test //...` green; format + lint; PR
